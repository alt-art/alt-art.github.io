import styled from 'styled-components';

const Desc = styled.p`
  max-width: 30rem;
  font-size: 1.2rem;
  margin: 0;
  padding: 0 4rem;
  @media (max-width: 768px) {
    padding: 0 2rem;
  }
`;

export default Desc;
